# The base URL of the deployed backend server (e.g. Render, Railway, or VPS)
# Example: https://devcollab-backend.onrender.com
# If left empty, it will fall back to local hosting or offline sandbox mode.
VITE_API_URL=
